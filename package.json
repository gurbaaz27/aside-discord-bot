{
  "name": "aside-discord-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use Aside browser agent sessions from Discord threads",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "discord.js": "^14.26.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
